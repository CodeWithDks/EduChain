[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "educhain"
version = "0.1.0"
description = "Educational AI framework"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["educhain*"]